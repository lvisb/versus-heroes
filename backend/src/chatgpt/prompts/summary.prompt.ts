export const summaryPrompt = (charName: string) =>
  `Summarize ${charName} with 300 characters, but generate a JSON with the name of the character, a summary propertie, and specify if is a villain or a heroe.`
